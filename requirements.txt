# ==========================================
# AiSprayer 依赖（兼容旧命令 pip install -r requirements.txt）
# ==========================================
#
# 推荐按场景使用拆分文件（见 README「环境」）：
#   视觉开发 (Python 3.10+) : requirements-common.txt + requirements-vision.txt
#   机械臂联调 (Python 3.8) : requirements-common.txt + requirements-robot.txt
#
# 本文件等价于「common + vision」，适用于 Python 3.10+ 单机全功能开发。
# Python 3.8 请勿直接安装本文件（scipy/torch 等无合适 wheel）。

-r requirements-common.txt
-r requirements-vision.txt